s = input()
d = []
for i in s:
    d.append(i)
print(d)
s2 = ''
k = 0
for i in range(1, len(d)):
    if d[i] == d[i-1]:
        k += 1
    else:
        s2 += str(k+1) + d[i-1]
        k = 0
if d[-1] != d[-2]:
    s2 += '1' + d[-1]
if k != 0:
    s2 += str(k+1) + d[-1]
print(s2)
